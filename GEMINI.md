This is a dockerized Laravel application for local Laravel package development. I'm using it to work on the Laravel Forum package, which is loaded locally (the directory has been added to this workspace - its path is /home/rick/Projects/Code/laravel-packages/laravel-forum).

The application is running at http://localhost:8000 via Docker (using Podman).